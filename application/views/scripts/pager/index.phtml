<!-- Exibe os itens da página atual -->
<?php foreach ($this->paginator as $item) : ?>
    O número é <?php echo $item; ?><hr>
<?php endforeach; ?>

<!-- Exibe os controles de paginação -->
<?php echo $this->paginationControl(
    $this->paginator,
    'Sliding', // Tipo de paginação (Sliding, Elastic, etc.)
    'partials/pagination.phtml' // Partial para renderizar os controles
); ?>